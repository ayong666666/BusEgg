spring:
  datasource:
    url: jdbc:mysql://localhost:3306/busegg_site_dev?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: your_mysql_password

busegg:
  jwt:
    secret: change-this-local-development-secret-at-least-32-characters
    expire-hours: 24
  init-admin:
    username: bugegg
    password: bugegg
    nickname: 初始管理员

